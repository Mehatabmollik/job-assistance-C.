#include<stdio.h>
int main()
{
    int x=9,y=9;
    if((x>y))
        printf("%d X is Big",x);
    else if((x<y))
        printf("%d y is Big",y);
    else
        printf("Both are Equal");
    return 0;
}
